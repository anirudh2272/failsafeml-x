# model.tar.gz placeholder

A real SageMaker deployment normally packages model artifacts into `model.tar.gz`.

This repository intentionally does not include binary model artifacts or cloud credentials. Use this placeholder to document where a packaged model artifact would be placed after local validation.
