[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"

[project]
name = "failsafeml-x"
version = "0.1.0"
description = "Self-healing reliability layer for ML systems - Milestone 8 final packaged prototype"
requires-python = ">=3.10"
